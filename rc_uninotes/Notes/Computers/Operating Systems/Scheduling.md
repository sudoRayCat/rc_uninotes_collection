



A CPU scheduler can be of two types
- Preemptive
- Non-preemptive

A task in a preemptive CPU can lose its turn in the CPU before it exits or switches itself to a waiting state.

A task is a non-preemptive CPU will keep its turn until it exits or switches itself to a waiting state.